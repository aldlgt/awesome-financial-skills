name: Candidate discovery

on:
  schedule:
    - cron: '0 8 * * 1' # 每周一 UTC 08:00
  workflow_dispatch:

jobs:
  discover:
    runs-on: ubuntu-latest
    env:
      DRY_RUN: "1"
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      - name: Install deps
        run: pip install PyGithub requests
      - name: Run discovery script
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          python tools/fetch_candidates.py
      - name: Create candidate PRs
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          python tools/create_candidate_prs.py
      - name: Run discovery script
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          python tools/fetch_candidates.py

      - name: Apply candidates to categories (dry-run by default)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          DRY_RUN: "1"   # 先测试，设为 "0" 以真正提交和创建 PR
        run: |
          python tools/apply_candidates.py
